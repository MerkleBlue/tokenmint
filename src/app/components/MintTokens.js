import React from 'react';
import { FormGroup } from '@material-ui/core';

const MintTokens = () => {
  return (
    <FormGroup>
      <p>This is a placeholder for MintTokens</p>
    </FormGroup>
  );
};

export default MintTokens;
