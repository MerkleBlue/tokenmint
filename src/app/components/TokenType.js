import React from 'react';
import { FormGroup } from '@material-ui/core';

const TokenType = () => {
  return (
    <FormGroup>
      <p>This is a placeholder for TokenType</p>
    </FormGroup>
  );
};

export default TokenType;
